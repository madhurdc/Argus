export const alerts = [];
/*
    alerts = [
        {
            type: string,
            message: string
        }
    ]

    type = 'MAIN_STATUS' || 'NEW_INCIDENT' || 'COMPONENT_STATUS' || 'RESOLVED_INCIDENT' || 'IMPACT_CHANGE'
*/